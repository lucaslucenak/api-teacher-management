spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/teacher-management
    username: postgres
    password: 123456
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true